# A5 灵山胜境 AI 数字人导览开发交接总结 第二版

日期：2026-06-02  
范围：`v1/backend`、`v1/frontend`、`v1/knowledge`、前后端联调与下一阶段前端体验优化  
延续文档：第一版见 `D:\ws01\v1\backend-handoff-summary-zh.md`

## 1. 当前状态总览

本阶段已经从“后端 P0 可跑通”推进到“结构化数据库优先问答 + 联网补充 + 前后端真实联调”的阶段。当前系统已经不再只是第一版 Mock 演示，而是具备真实后端、真实 MIMO 大模型调用、后端 TTS、知识库入库、管理后台运营视图、问答日志管理和来源展示的完整闭环。

当前可用能力：

- 后端 FastAPI 服务、SQLite 数据库、Swagger、统一错误格式。
- 灵山胜境和拈花湾结构化景点数据入库，共 22 个景点。
- `v1/knowledge` 派生知识包已从两个 Word 原始资料生成并入库。
- AI 导游问答优先检索数据库和知识库，不足时再走 MIMO 联网补充。
- 联网补充支持来源卡片展示，并支持管理后台审核候选事实。
- 后端 TTS 接入，前端支持“播放讲解 / 停止讲解”，避免多段音频叠播。
- 管理后台支持知识库状态、联网事实候选审核、问答日志删除、批量删除、全选取消。
- 前端已去掉大量 P0 演示标识，品牌和文案统一为“灵山胜境景点 AI 数字人导览”方向。
- 景点数量已从旧版 5 个同步为后端数据库真实数量 22 个。

最近一次验证：

- 后端完整测试：`78 passed`
- 前端构建：`npm run build` 通过

## 2. 关键目录与文档索引

核心目录：

- 后端：`D:\ws01\v1\backend`
- 前端：`D:\ws01\v1\frontend`
- 原始资料包：`D:\ws01\v1\Scenic Area Public Information Package`
- 派生知识库：`D:\ws01\v1\knowledge`
- SQLite 数据库：`D:\ws01\v1\backend\data\app.db`

关键文档：

- 第一版交接：`D:\ws01\v1\backend-handoff-summary-zh.md`
- 后端 README：`D:\ws01\v1\backend\README.md`
- 前端后端早期交接：`D:\ws01\v1\frontend\FRONTEND_BACKEND_HANDOFF.md`
- 后端 P0 PRD：`D:\ws01\v1\frontend\.scratch\backend-p0\PRD.md`
- 结构化数据库优先问答 PRD：`D:\ws01\v1\frontend\.scratch\structured-db-first-qa\PRD.md`
- 结构化问答验证文档：`D:\ws01\v1\frontend\.scratch\verification-docs\structured-qa-feature-verification.md`

## 3. 本阶段主要开发内容

### 3.1 知识源与数据库同步

当前知识来源主要是：

- `灵山胜境 景点结构化数据集.docx`
- `灵山胜境：历史、文化、景点特色与个性化游览指南.docx`

已完成：

- 从结构化景点 Word 中解析出 22 个景点：
  - `LS-001` 到 `LS-016`
  - `NH-001` 到 `NH-006`
- 生成 `v1/knowledge/docs` 下的 Markdown 文档：
  - 22 份景点 Markdown
  - 1 份历史文化与游览指南 Markdown
- 生成 `v1/knowledge/curated` 下的结构化 JSON：
  - `spots.json`
  - `facts.json`
  - `aliases.json`
  - `tags.json`
  - `source_refs.json`
  - `approved_web_sources.json`
  - `approved_web_facts.json`
- 后端启动时将派生知识包导入 SQLite。
- 管理后台知识库页面显示：
  - 知识文档 25 份
  - 派生文档 23 份
  - 景点文档 22 份
  - 知识切片 51 块

已验证 `灵山胜境：历史、文化、景点特色与个性化游览指南.docx` 的全文进入知识库切片，且门票、路线、历史、文化、景点特色等信息可被检索。

### 3.2 AI 导游问答链路

当前问答主流程：

1. 前端 `/guide` 提交用户问题到 `/api/chat`。
2. 后端进行问题分类：
   - 景区事实
   - 景点讲解
   - 路线建议
   - 服务咨询
   - 高风险实时信息
   - 外部事实问题
   - 日常友好互动
3. 分类阶段使用规则关键词、数据库实体别名、必要时 MIMO LLM 分类。
4. 检索阶段优先查：
   - 结构化事实 `spot_fact`
   - 已审核联网补充 `approved_web_fact`
   - 景点标签和简介
   - 知识文档切片 `knowledge_chunk`
5. 若数据库和文档证据不足，再触发 MIMO 联网搜索。
6. MIMO 生成自然导游话术。
7. 后端返回：
   - answer
   - sources
   - metrics
   - tts job/audio 状态
   - degraded/mode 信息
8. 前端展示回答、来源卡片、语音播放按钮。

当前已修复的重要问答问题：

- 天气问题曾经无法稳定拿到 MIMO `annotations`，已增加正文 URL/权威来源名回退解析。
- “景区票价 / 门票收费情况”曾经错误走联网失败，已改为优先使用指南文档中的票价信息：
  - 成人票 210 元
  - 半价票 105 元
  - 免票政策
- 票价问题失败文案不再错误提示“打开天气应用”。
- 景区相关事实问题数据库优先，普通外部问题可走联网互动。
- 若问具体景点缺少结构化事实，例如单独问某个景点独立票价，仍保留联网补充路径。

### 3.3 联网补充与事实审核

当前联网补充基于 MIMO Web Search 能力。

实现要点：

- 使用同一个 MIMO API 配置，不单独引入第三方搜索 API。
- 请求中带 `forced_search` / `force_search`，尽量强制触发联网搜索。
- 解析优先级：
  - `annotations.url_citation`
  - 正文中的 URL
  - 正文中明确出现的权威来源名称，例如中国天气网、无锡市气象局
- 来源过滤区分：
  - official
  - authoritative
  - ordinary
- 天气、票价、开放时间等高风险问题只接受更可信来源。
- 景区实体相关联网结果会生成 `web_fact_candidate`，进入管理后台审核。

管理后台候选操作：

- 补充：作为已审核联网补充，后续问答可引用但不覆盖数据库事实。
- 入库：作为官方结构化事实进入数据库，优先级更高。
- 拒绝：后续同类信息不应被采用。
- 忽略：不采用，但不等同于明确错误。

### 3.4 后端 TTS 与前端播放体验

当前 TTS 由后端处理，前端只消费音频 URL 或 TTS job 状态。

已完成优化：

- 开头欢迎语使用本地固定音频，避免每次实时生成导致割裂。
- 回答生成后显示“播放讲解”按钮。
- 点击播放后按钮变为“停止讲解”。
- 再次点击同一按钮会停止当前音频。
- 点击另一条回答的播放按钮，会先停止上一条音频，再播放新的音频。
- 避免多个语音同时播放混在一起。

相关文件：

- `D:\ws01\v1\frontend\src\components\guide\AudioButton.tsx`
- `D:\ws01\v1\frontend\src\components\guide\welcomeMessage.ts`
- `D:\ws01\v1\frontend\public\audio\guide-welcome.mp3`

### 3.5 前端产品化调整

已完成：

- 去掉部分 P0 演示标签和 Mock 字样。
- 前端默认调用真实后端，不需要手动设置 `VITE_USE_MOCK_API=false`。
- 首页景点数量使用后端真实 `/api/spots/summary`。
- 景点页显示“已同步 22 个入库景点”。
- 管理后台知识库页面显示真实知识文档统计。
- 问答日志支持单条删除、勾选批量删除、全选删除，并支持再次点击全选取消。
- AI 导游聊天框长文本自动换行。
- 来源卡片去重，减少重复资料卡。

## 4. 当前运行与手动联调方法

### 4.1 启动后端

PowerShell：

```powershell
cd D:\ws01\v1\backend
.\.venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

健康检查：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
```

Swagger：

```text
http://127.0.0.1:8000/docs
```

注意：

- 根路径 `http://127.0.0.1:8000/` 返回 `{"detail":"Not Found"}` 是正常现象。
- `.env` 位于 `D:\ws01\v1\backend\.env`。
- 文档中不要记录真实 API Key；只确认 `MIMO_API_KEY`、`LLM_MODE`、`TTS_MODE` 等配置项存在即可。

### 4.2 启动前端

PowerShell：

```powershell
cd D:\ws01\v1\frontend
npm run dev
```

常用访问地址：

```text
游客端首页：http://127.0.0.1:5173/
景点列表：http://127.0.0.1:5173/spots
AI 导游：http://127.0.0.1:5173/guide
管理后台：http://127.0.0.1:5174/
知识库管理：http://127.0.0.1:5174/knowledge
问答日志：http://127.0.0.1:5174/logs
```

### 4.3 重建知识库

当原始 Word 有变化，先重新派生知识包：

```powershell
cd D:\ws01\v1\backend
.\.venv\Scripts\python.exe scripts\derive_knowledge_package.py --source "..\Scenic Area Public Information Package" --output "..\knowledge"
```

再启动后端或调用重建接口：

```powershell
Invoke-RestMethod -Method Post http://127.0.0.1:8000/api/knowledge/rebuild
```

后台知识库页面应看到索引数量更新。

### 4.4 手动联调推荐问题

景点讲解：

- 灵山大佛有什么看点？
- 灵山梵宫有什么特色？
- 九龙灌浴适合带小朋友看吗？
- 哪里适合拍照？

结构化事实：

- 灵山大佛多高？
- 景区票价是多少？
- 我想咨询一下景区门票收费情况
- 五明桥收费吗？

联网补充：

- 今天无锡天气如何？
- 北京今天的天气如何？
- 梵宫今天开放时间变了吗？

兜底与友好互动：

- 你好
- 我有点累，接下来适合去哪？
- 你能扮演一个幽默的导游讲讲灵山大佛吗？

管理后台：

- 在 `/admin/knowledge` 查看知识文档、景点文档、索引状态。
- 在 `/admin/logs` 删除单条问答日志、批量删除、全选后取消全选。
- 在联网事实候选里测试“补充 / 入库 / 拒绝 / 忽略”。

### 4.5 自动验证命令

后端：

```powershell
cd D:\ws01\v1\backend
.\.venv\Scripts\python.exe -m pytest -q
```

前端：

```powershell
cd D:\ws01\v1\frontend
npm run build
```

当前最近一次结果：

- 后端：`78 passed`
- 前端：构建通过

## 5. 当前设计限制与注意事项

### 5.1 文档入库不等于全部结构化

指南 Word 已完整进入数据库切片，但不是所有内容都拆成 `spot_fact` 结构化字段。当前策略是：

- 明确景点事实优先结构化表。
- 长段历史文化、路线建议、游览贴士保留为文档切片。
- 票价这类文档事实已经通过检索加权支持直接回答。

后续如果追求更高性能，可以继续把票务、开放时间、演艺时间、路线模板、亲子/摄影/休闲标签抽成结构化表。

### 5.2 MIMO 联网返回格式存在波动

MIMO Web Search 有时返回 `annotations`，有时只在正文中写 URL 或来源名。当前后端已做兼容，但仍建议后续继续观察真实日志。

### 5.3 前端视觉仍偏功能型

虽然已去掉大量 P0 演示痕迹，但当前前端仍以表格、卡片、文字为主。景点卡片缺少真实风景照，首页和景点列表的观感还不够像面向游客的正式产品。

## 6. 后续开发方向

### 6.1 前端界面重点优化

下一阶段建议重点投入前端体验，方向如下：

1. 景点资料卡加入真实风景照

   为 22 个景点收集或整理图片资产，用于：

   - 首页热门景点卡片
   - 景点列表卡片
   - 景点详情页顶部大图
   - AI 回答来源卡片可选缩略图

   推荐先做一张映射表：

   ```text
   spot_id -> image_url / local asset path / copyright note
   ```

   注意图片来源版权。优先使用景区官方公开素材、用户提供素材或可授权图片。

2. 景点列表升级为游客浏览体验

   当前列表偏资料库展示，后续可优化为：

   - 图片主视觉
   - 标签筛选：亲子、摄影、文化、室内、演艺
   - 推荐停留时间
   - “适合现在去 / 适合雨天 / 适合带老人”提示
   - 点击卡片进入沉浸式详情页

3. 景点详情页增强

   可加入：

   - 顶部风景照或轮播图
   - 核心看点摘要
   - 最佳拍照角度
   - 推荐停留时间
   - 附近相关景点
   - 一键问 AI：“帮我讲讲这个景点”

4. AI 导游页产品化

   当前聊天功能可用，后续可优化：

   - 把来源卡片做成更轻量的“依据资料”折叠区
   - 常用问题推荐按当前上下文变化
   - 加入“正在检索 / 正在联网 / 正在生成语音”阶段提示
   - 语音播放状态和数字人状态更细腻联动

5. 管理后台更贴近运营

   后台可继续增强：

   - 景点图片上传/维护
   - 知识文档版本号和更新时间
   - 联网候选事实的批量审核
   - 问答日志按意图、来源、是否联网筛选
   - 常见问题统计

### 6.2 后端与知识库后续方向

1. 继续结构化提取指南 Word

   可从指南中进一步抽取：

   - 票价政策
   - 演艺时间
   - 亲子路线
   - 摄影路线
   - 老人友好路线
   - 实用贴士

2. 引入向量检索预留接口

   当前是两级检索和关键词排序，后续可以接 embedding/vector store：

   - 保持当前关键词检索作为稳定 fallback。
   - 向量召回用于语义问题。
   - 最终融合排序。

3. 提升问答评测集

   当前已有结构化 QA evaluation set，可继续扩展到：

   - 景点介绍
   - 票价服务
   - 天气联网
   - 情绪安抚
   - 幻觉检测

4. 真实登录和权限

   P0 明确不做真实登录。后续如进入准生产，应补：

   - 管理端登录
   - 管理 API token 强制开启
   - 审核操作记录操作者

## 7. 建议使用技能

下一位继续开发的 agent 建议按任务选择：

- `diagnose`：排查问答异常、联网异常、前后端联调不一致、性能变慢。
- `tdd`：继续开发结构化字段、图片接口、前端交互时保持回归测试。
- `improve-codebase-architecture`：做前端产品化重构、景点图片资产模型、知识检索模块整理。
- `zoom-out`：需要重新理解当前问答链路、知识库结构、前后端边界时使用。
- `to-prd` / `to-issues`：如果要把“前端视觉升级 + 景点图片资产管理”拆成正式迭代任务。
- `handoff`：下一阶段结束后继续追加第三版交接总结。

## 8. 建议下一步迭代拆分

建议下一阶段先从“游客端视觉体验升级”开始，而不是继续堆后端功能。

优先级建议：

1. 收集 22 个景点的可用风景照，并建立图片来源清单。
2. 给 `ScenicSpot` 增加稳定 `image_url` 或本地图片映射。
3. 优化景点列表卡片，让每个景点有图、有标签、有停留时间、有一句核心看点。
4. 优化景点详情页，形成更像正式旅游产品的介绍页。
5. 调整首页热门景点和推荐路线视觉，使其从“数据演示”转向“游客浏览”。
6. 最后再回到管理后台，补景点图片维护能力。

这一轮如果做好，项目会从“技术功能可用”明显提升到“游客愿意使用”的观感阶段。
